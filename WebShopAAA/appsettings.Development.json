{
  // we can Use Logging Default level to Warning and save the warning i an Text File ! 
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
